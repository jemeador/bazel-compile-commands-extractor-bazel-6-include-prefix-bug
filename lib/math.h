#pragma once

// NOTE! Using includes means headers are pulled in with -isystem, which causes
// important compiler warnings to be ignored in headers, like not returning a
// value here!
inline int my_add (int a, int b) { }
